# Assets

Put your app icon here as `Icon.png` (square, transparent PNG, >= 256px) and point Intune's app *Logo* at it when
you create the Win32 app. This repo ships no icon on purpose - use your own organization's logo.

The launcher does not reference this file directly; the icon is set on the Intune app, not inside the package.
